apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-test
spec:
  replicas: 3
  selector:
    matchLabels:
      app: go-test
  template:
    metadata:
      labels:
        app: go-test
    spec:
      containers:
      - name: go-test
        image: krobus00/go-test-service:latest
        imagePullPolicy: IfNotPresent
        ports:
          - containerPort: 3000